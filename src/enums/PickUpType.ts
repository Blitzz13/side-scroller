export enum PickUpType {
  AMMO = "ammo",
  HEALTH = "health",
}

export enum PickupType {
  WEAPON = "weapon",
  AMMO = "ammo",
  HEALTH = "health",
  SHIELD = "shield",
  NONE = "-",
  BLUE_KEYCARD = "blue_keycard",
  RED_KEYCARD = "red_keycard",
  GREEN_KEYCARD = "green_keycard",
}
